import { describe, expect, it } from "vitest";
import { 格式化主日期值, 解析主日期值 } from "../src/界面/主日期输入";

const 最小日期 = "1900-01-01";
const 最大日期 = "2100-12-31";

describe("主日期输入", () => {
  it("接受完整合法的年月日", () => {
    expect(解析主日期值("2027-12-25", 最小日期, 最大日期)).toEqual(new Date(2027, 11, 25));
  });

  it.each(["202", "2027-0", "2027-02-30", "1899-12-31", "2101-01-01", ""])(
    "未完成、非法或越界值不会进入日历状态：%s",
    (值) => expect(解析主日期值(值, 最小日期, 最大日期)).toBeNull(),
  );

  it("外部月格选择可稳定同步回日期输入", () => {
    expect(格式化主日期值(new Date(2027, 11, 25))).toBe("2027-12-25");
  });
});
